"""Shared pytest fixtures.

Currently empty — kept so tests can rely on a stable conftest discovery
point and so future fixtures (Session factory, DB sandbox) have a home.
"""

from __future__ import annotations
