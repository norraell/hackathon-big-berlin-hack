"""Claims persistence layer.

SQLAlchemy models (``models.py``) and a small service (``service.py``) that
the LLM tool dispatcher uses to materialise a claim and attach the
transcript. Implementation lands in a later task.
"""
