"""Dialog state.

Holds per-call session objects and the legal state-machine transitions for
the intake conversation. ``app/dialog/session.py`` is the single source of
truth for session state per CLAUDE.md §5.2.
"""
